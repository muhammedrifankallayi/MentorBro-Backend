const studentController = require('./student');
const reviewerController = require('./reviewer');
const adminController = require('./admin');

module.exports = {
    studentController,
    reviewerController,
    adminController,
};
